# Mobile resource intake: September 24, 2026

Three user-supplied files were registered for the v1.4 media-kit release. Existing logo, wordmark, social, animation, email, payment-method and WorkAdventure paths are unchanged.

## Provenance

| Original upload | Repository destination | Original SHA-256 |
| --- | --- | --- |
| `Openline-Mobile-Screens.fig` | `figma/mobile/openline-mobile-screens-2026-09-24.fig` | `68cc3bd0f8dbed41c4c894bf777880eb83087690f256e0ec60abc8b7b4333e07` |
| `openline-app-icons-2.zip` | `app-icons/` | `b3ae9e94a34297d8d1f364a1b57a1d8c954be22fdac6e27e9873ce906ec37519` |
| `openline-mobile-app-screens-2026-09-24-3.zip` | `mobile/app-screens/` | `d7e6496717de08cf1da7898f52cd25137826dd9d5fb3bd2cc0cda21241305e61` |

## Packaging decisions

- **Icons:** Preserve the native catalog, density folder names and PNGs. The icon pack contains 15 iOS PNGs and nine Android PNGs; `Contents.json` maps 18 iOS entries, with some sizes sharing a file.
- **Figma:** Preserve the original `.fig` bytes and extract its embedded thumbnail for the repository preview.
- **React:** Preserve component names and hashed image/SVG dependencies. Add a type-check command and ignore local dependencies/build output.
- **Preview:** Use a relative Vite base (`./`) and rebuild `preview/` so it can be hosted under a subdirectory, not only at a domain root.
- **Source fixes:** Add Vite's PNG import declarations and define the missing `ALL_PAYMENT_METHODS` demo registry from the 22 supplied logo components. Without this registry, opening the checkout method picker throws a runtime error.
- **Deep links:** Match exact screen names before partial names so `?screen=Me` opens the account screen, rather than the earlier `Payment success + code` screen.
- **Dependency maintenance:** Update Vite from 6.3.5 to 6.4.3 within the existing major version and refresh the lockfile in response to the dependency audit.
- **Documentation:** Keep the supplied implementation notes, add navigation and maintenance instructions, and distinguish mock UI from production functionality.

## Review boundaries

The source text was checked for common credential patterns; its eSIM values are explicitly identified as mock data in the supplied README. This is not a security audit or an independent verification of trademark licenses.

The Figma archive was checked for integrity and metadata, not semantically audited through a Figma reimport. Platform icon dimensions and iOS opacity were checked locally; store acceptance and native-device integration remain the consuming app team's responsibility.

## Validation results

Completed on September 24, 2026:

- `npm run typecheck`: passed.
- `npm run build -- --outDir preview`: passed.
- `npm audit`: zero known vulnerabilities reported for the installed dependency set at the time of the check.
- Browser smoke test: all 19 named screen deep links render; the desktop gallery and 390x844 mobile device view render.
- Checkout: the payment-method sheet opens, displays 22 demo choices and closes after selecting Visa without a runtime error.
- Navigation: the Next screen button advances from Checkout to Payment success.
- Assets: no failed HTTP responses or broken loaded images during those checks; no uncaught browser page errors.
- Native resources: every Xcode catalog reference resolves, with matching pixel dimensions and opaque pixels; all Android PNGs are readable at their documented dimensions.
- Figma: archive CRC check and byte-for-byte comparison with the upload pass.

These are targeted smoke checks, not exhaustive UI, accessibility, native-platform or production-integration tests. The original `.fig` is unchanged; the React preview contains the explicitly documented packaging and source fixes.
